---
description: Write this session up as one standalone revision note in Obsidian
argument-hint: "[note title or vault/folder hint]"
---

Use the `recap` skill to distil this teaching session into ONE standalone revision note in the user's Obsidian vault.

Follow the skill exactly: offer the single recall prompt first, run `vault.mjs list` and agree on a destination rather than guessing one, publish any diagrams from `viz/` with `vault.mjs attach`, and write a note that reads standalone — not a transcript.

Extra context from the user (may be empty): $ARGUMENTS
