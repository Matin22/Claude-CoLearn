---
description: Write this session up as one standalone revision note
argument-hint: "[note title or vault/folder hint]"
---

Use the `recap` skill to distil this teaching session into ONE standalone revision note — in the user's Obsidian vault, or a plain local file if they're not using Obsidian.

Follow the skill exactly: offer the single recall prompt first, run `vault.mjs list` and agree on a destination rather than guessing one — **"just this project, no Obsidian" is always a real option, not a fallback of last resort** — publish any diagrams from `viz/` (via `vault.mjs attach` for a vault, or a plain relative link for a local note), and write a note that reads standalone — not a transcript.

Extra context from the user (may be empty): $ARGUMENTS
